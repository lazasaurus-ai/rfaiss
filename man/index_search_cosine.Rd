% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{index_search_cosine}
\alias{index_search_cosine}
\title{Search using IndexFlatIP (for cosine use; returned "distances" are inner-products / cosine scores)}
\usage{
index_search_cosine(handle, q, k = 5L)
}
\arguments{
\item{handle}{rfaiss index handle (created by \code{\link[=create_flat_cosine]{create_flat_cosine()}}).}

\item{q}{Query matrix (rows = queries). Should be normalized the same way you normalized index vectors.}

\item{k}{Number of neighbors to return.}
}
\value{
A list with \code{distances} (similarities) and \code{indices}.
}
\description{
NOTE: IndexFlatIP returns inner-products (higher = better). To maintain compatibility with the
L2 API we still return a \code{distances} matrix — in the cosine case these are similarity scores.
}
