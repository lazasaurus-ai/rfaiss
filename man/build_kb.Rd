% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_kb.R
\name{build_kb}
\alias{build_kb}
\title{Build a KB (FAISS index + manifest) from a directory (convenience wrapper)}
\usage{
build_kb(
  dir,
  out_prefix = NULL,
  use_tmp = FALSE,
  overwrite = FALSE,
  readme = TRUE,
  ...
)
}
\arguments{
\item{dir}{Directory containing source files to index.}

\item{out_prefix}{Optional path prefix for the generated index and manifest.
If NULL (default) the wrapper will create a prefix next to \code{dir}:
\code{file.path(dir, paste0("kb_", timestamp))}. If \code{use_tmp = TRUE},
a temporary prefix (via \code{tempfile()}) will be used.}

\item{use_tmp}{Logical; if TRUE forces a tempfile() prefix (useful for quick tests).}

\item{overwrite}{Logical; if FALSE (default) and an index/manifest already exist at
the computed prefix, this function will error. Set TRUE to replace.}

\item{readme}{Logical; write a small README markdown file beside the index/manifest.}

\item{...}{Additional arguments are forwarded to \code{build_flatl2_from_dir()}:
e.g. \code{pattern}, \code{chunk_chars}, \code{overlap}, \code{model}, \code{python}.}
}
\value{
Invisibly returns the result produced by \code{build_flatl2_from_dir()},
with an added element \code$readme_path (if \code{readme = TRUE}).
}
\description{
A convenience wrapper around \code{build_flatl2_from_dir()} that chooses a
sensible default \code{out_prefix} (by default next to the source files),
writes a small README describing the KB, and returns the result invisibly.
}
\examples{
\dontrun{
# build KB next to ./docs
build_kb("docs")

# build KB into /tmp for quick test
build_kb("docs", use_tmp = TRUE)
}
}
